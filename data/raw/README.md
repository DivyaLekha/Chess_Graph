# PGN data

Put the PGN dataset you are licensed to use here.

The repository intentionally does not include a large chess dataset or model checkpoints.

Expected file:

`data/raw/games.pgn`

A tiny `sample.pgn` is included only for testing the pipeline.
